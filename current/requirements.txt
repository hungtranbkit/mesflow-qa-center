Flask>=3.0,<4
requests>=2.31,<3
psutil>=5.9,<7
playwright>=1.45,<2
Werkzeug>=3.0,<4
pytest>=8.0,<9
psycopg[binary]>=3.2,<4
# qualification/esp_hil.py: real USB/serial detection + read-only identity
# capture for a physically-connected ESP32 kiosk. Found missing by hand --
# esp_hil correctly reported BLOCKED("pyserial not installed") rather than
# silently skipping, which is exactly how an honest missing dependency
# should surface, but the dependency itself was still a real gap to close.
pyserial>=3.5,<4
