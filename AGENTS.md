# qa-center

Read and obey the workspace rules first:

`../AGENTS.md`

Project directory:
`qa-center/`

Do not modify sibling projects unless the task explicitly requires a cross-project change.

Before release or promotion changes, read `../docs/operations/DEPLOY_AGENT_QA_TUTORIAL_TROUBLESHOOTING.md`.

All future QA Center UI work must follow `docs/UI_STANDARD.md`.

No new arbitrary spacing, color, radius, or control style may be introduced when an existing token or component already covers the use case. Desktop 1366x768 plus a mobile smoke test and screenshot review are required for non-trivial UI changes.
